var expect = require("chai").expect;
var should = require('chai').should();
var assert = require('assert');

// check that you can recognize interfaces, and collections 

describe(".implement(<Interface>)", function() {
  it("return object when passed an object that implements <Interface>", function() {
    
    
    
    expect(true).to.be.ok;
  })
})